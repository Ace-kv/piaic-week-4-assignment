"use strict";
// TODO Write a program that takes input and calculates the volume of a cube.
let sideInput = 5;
let volCube = (side) => side ** 3;
console.log(`Volume of the cube with a side of ${sideInput} is:`, volCube(sideInput));
