// TODO Calculate the area of a rectangle
let widthR = 5
let lengthR = 6

let rectangleArea = (width: number, length: number): number => width * length

console.log(`The rectangle with width ${widthR} and length ${lengthR} is:`, rectangleArea(widthR, lengthR));
